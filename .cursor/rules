# 📋 Правила проекта "Доходометр"

> **ОБЯЗАТЕЛЬНО К ИЗУЧЕНИЮ И СОБЛЮДЕНИЮ**

## 🎯 ФИЛОСОФИЯ ПРОЕКТА

"Доходометр" - профессиональный сервис учета инвестиций для российских инвесторов с полным соответствием законодательству РФ. 

**Принципы:** Качество, Безопасность, Профессионализм, Соответствие законам РФ.

---

## 🎨 ДИЗАЙН-СИСТЕМА (КРИТИЧЕСКИ ВАЖНО!)

⚠️ **ПЕРЕД ЛЮБЫМИ ИЗМЕНЕНИЯМИ ДИЗАЙНА ОБЯЗАТЕЛЬНО ЧИТАЙ `DESIGN_SYSTEM.md`**

### Концепция дизайна:
- **"Светлый кабинет"** - профессиональный аналитический дизайн
- Воздушные карточки с мягкими тенями
- Микро-диаграммы и параллакс-эффекты
- Ощущение рабочего стола финансового аналитика

### Цветовая палитра (СТРОГО):
```css
--primary: #1F3B35          /* Глубоко-изумрудный */
--accent-1: #C79A63         /* Медно-песочный */
--accent-2: #63B8A7         /* Свежий мятный */
--neutral-light: #F8F9F8    /* Нейтральный светлый */
--neutral-dark: #2C2E2D     /* Нейтральный тёмный */
```

### Типографика:
- **Основной:** Manrope (текст, навигация, описания)
- **Акцентный:** Roboto Serif Narrow (заголовки, цифры)

### 🚫 ДИЗАЙН - ЗАПРЕЩЕНО:
- ❌ Новые цвета без согласования
- ❌ Другие шрифты кроме указанных
- ❌ Компоненты без адаптивности
- ❌ Тяжелые анимации (>60fps)
- ❌ Нарушение контрастности (WCAG)

### ✅ ДИЗАЙН - ОБЯЗАТЕЛЬНО:
- ✅ CSS-переменные для всех стилей
- ✅ Компонентный подход (shadcn/ui)
- ✅ Поддержка темной темы
- ✅ Адаптивность (desktop/tablet/mobile)
- ✅ Микро-анимации диаграмм
- ✅ Доступность (a11y)

---

## 💻 ПРАВИЛА НАПИСАНИЯ КОДА

### 🐍 Backend (FastAPI + Python)

#### Архитектура БД:
```python
# ✅ ПРАВИЛЬНО - Синхронная архитектура
from app.core.database_sync import get_db
from sqlalchemy.orm import Session

def endpoint(db: Session = Depends(get_db)):
    return db.query(Model).all()

# ❌ НЕПРАВИЛЬНО - Асинхронная архитектура
from app.core.database import get_db
from sqlalchemy.ext.asyncio import AsyncSession

async def endpoint(db: AsyncSession = Depends(get_db)):
    return await db.execute(select(Model))
```

#### Стиль кода Python:
```python
# ✅ Правильно
def calculate_ndfl(
    income: Decimal,
    is_resident: bool = True,
    tax_year: int = 2024
) -> TaxResult:
    """Расчет НДФЛ в соответствии с НК РФ."""
    rate = Decimal("0.13") if is_resident else Decimal("0.30")
    return TaxResult(
        income=income,
        tax_amount=income * rate,
        rate=rate
    )

# ❌ Неправильно - отсутствие типов и документации
def calc(income, resident=True):
    return income * 0.13 if resident else income * 0.30
```

#### Обязательные требования Python:
- ✅ Типизация для всех функций (typing)
- ✅ Docstrings для всех публичных методов
- ✅ Pydantic модели для валидации
- ✅ Обработка исключений
- ✅ Логирование важных операций
- ✅ Только синхронные функции (NO async/await)

### ⚛️ Frontend (Next.js + TypeScript)

#### Структура компонентов:
```typescript
// ✅ Правильно - типизированный компонент
interface PortfolioCardProps {
  portfolio: Portfolio;
  onEdit?: (id: number) => void;
  className?: string;
}

export function PortfolioCard({ 
  portfolio, 
  onEdit, 
  className 
}: PortfolioCardProps) {
  return (
    <Card className={cn("portfolio-card", className)}>
      <CardHeader>
        <CardTitle>{portfolio.name}</CardTitle>
      </CardHeader>
      <CardContent>
        {/* Контент с анимациями */}
      </CardContent>
    </Card>
  );
}

// ❌ Неправильно - без типов
export function Card({ data, onClick }) {
  return <div onClick={onClick}>{data.name}</div>;
}
```

#### API интеграция:
```typescript
// ✅ Правильно - централизованный API клиент
import { api } from '@/lib/api-client';

const { data, error } = useQuery({
  queryKey: ['portfolios'],
  queryFn: () => api.portfolios.getAll()
});

// ❌ Неправильно - прямые fetch запросы
const data = await fetch('/api/portfolios');
```

#### Обязательные требования Frontend:
- ✅ TypeScript strict mode
- ✅ Компонентная архитектура (shadcn/ui)
- ✅ CSS-переменные из дизайн-системы
- ✅ React Query для API запросов
- ✅ Обработка ошибок и loading состояний
- ✅ Адаптивность на всех устройствах
- ✅ Поддержка темной темы

---

## 🔧 ТЕХНИЧЕСКИЕ СТАНДАРТЫ

### 🐳 Docker:
```dockerfile
# ✅ Правильно - Production Dockerfile
FROM python:3.12-slim
LABEL maintainer="Dohodometr Team"
RUN groupadd -r appuser && useradd -r -g appuser appuser
USER appuser
HEALTHCHECK CMD curl -f http://localhost:8000/health

# ❌ Неправильно - без безопасности
FROM python:3.12
COPY . .
CMD ["python", "app.py"]
```

### 🔒 Безопасность:
```python
# ✅ Правильно - автогенерация секретов
SECRET_KEY: str = Field(default_factory=lambda: secrets.token_urlsafe(32))

# ❌ Неправильно - хардкод
SECRET_KEY = "my-secret-key"
```

### 🗃️ База данных:
- ✅ Синхронные подключения (database_sync)
- ✅ Миграции через Alembic
- ✅ Модели SQLAlchemy с типизацией
- ✅ Индексы для производительности

---

## 📝 ПРАВИЛА ДОРАБОТОК И ИЗМЕНЕНИЙ

### 🔄 Процесс внесения изменений:

1. **Планирование:**
   - Читать DESIGN_SYSTEM.md
   - Проверить соответствие архитектуре
   - Оценить влияние на производительность

2. **Разработка:**
   - Следовать стилю кода
   - Добавлять типизацию
   - Писать документацию

3. **Тестирование:**
   - Проверить синтаксис: `python3 -m py_compile`
   - Тестировать на всех устройствах
   - Проверить темную тему

4. **Коммит:**
   - Осмысленное сообщение
   - Атомарные изменения
   - Следовать conventional commits

### 📋 Чек-лист перед коммитом:
```bash
# Обязательные проверки:
□ Соответствие дизайн-системе
□ Типизация TypeScript/Python
□ Синхронная архитектура БД
□ Адаптивность интерфейса
□ Поддержка темной темы
□ Безопасность (секреты, CORS)
□ Производительность
□ Доступность (a11y)
□ Синтаксис кода валиден
□ Документация обновлена
```

---

## 🚨 КРИТИЧЕСКИЕ ТРЕБОВАНИЯ

### ❌ ЗАПРЕЩЕНО:
- Асинхронные функции в Backend
- Новые цвета в дизайне
- Хардкод секретов/паролей
- Компоненты без адаптивности
- Код без типизации
- Изменения без тестирования
- Нарушение российского законодательства

### ✅ ОБЯЗАТЕЛЬНО:
- Читать .cursor/rules перед работой
- Следовать DESIGN_SYSTEM.md
- Типизировать весь код
- Обеспечивать безопасность
- Тестировать на всех устройствах
- Поддерживать производительность
- Соблюдать законы РФ

---

## 🎯 СПЕЦИФИКА ПРОЕКТА

### 🇷🇺 Российское законодательство:
- Соответствие 152-ФЗ (персональные данные)
- Учет требований валютного законодательства
- Правильный расчет НДФЛ (13%/30%)
- Поддержка ИИС (тип А и Б)
- Льгота долгосрочного владения (ЛДВ)

### 💼 Финансовая специфика:
- Точность расчетов (Decimal для денег)
- Валидация финансовых данных
- Безопасность финансовой информации
- Аудит всех операций

---

## 📊 МОНИТОРИНГ КАЧЕСТВА

### Автоматические проверки:
```bash
# Python синтаксис
python3 -m py_compile backend/app/**/*.py

# TypeScript проверка
npm run type-check

# Линтинг
npm run lint

# Безопасность
grep -r "password\|secret\|key" --exclude-dir=node_modules
```

### Метрики качества:
- 🎯 **Производительность:** Core Web Vitals
- 🔒 **Безопасность:** 0 уязвимостей
- 📱 **Адаптивность:** 100% устройств
- ♿ **Доступность:** WCAG 2.1 AA
- 🎨 **Дизайн:** 100% соответствие системе

---

## 🏆 ЦЕЛЬ: БЕЗУПРЕЧНОЕ КАЧЕСТВО

**"Доходометр" должен быть эталоном качества среди российских fintech-сервисов**

### Наши стандарты:
- 🎨 **Дизайн:** Профессиональный, интуитивный
- 💻 **Код:** Чистый, типизированный, безопасный  
- 🚀 **Производительность:** Быстрая загрузка и отклик
- 🔒 **Безопасность:** Максимальная защита данных
- 📱 **UX:** Удобство на всех устройствах
- 🇷🇺 **Соответствие:** 100% законам РФ

---

**Помни: Качество кода = качество продукта = доверие пользователей**

*Версия правил: 2.0.0*
*Последнее обновление: Декабрь 2024*
